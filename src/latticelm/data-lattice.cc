#include <latticelm/data-lattice.h>
#include <latticelm/macros.h>

using namespace latticelm;

vector<DataLatticePtr> DataLattice::ReadFromFile(const std::string & filename, SymbolSet & dict) {
  THROW_ERROR("DataLattice::ReadFromFile not implemented");
}
